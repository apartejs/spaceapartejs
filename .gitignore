node_modules/
dist/
.vite/

.DS_Store
*.local

# Internal working documents, kept out of the public repository on purpose.
# These are our own notes — framing, feasibility, product decisions, throwaway
# spikes — written in French for the two of us. The published repository is
# English, and it holds the product, not the conversation that produced it.
docs/
spikes/
.claude/

# Throwaway probes: generated Spaces and fixtures written while verifying a spike.
# They live in public/ so the dev server can serve them, which is exactly why they
# need naming here — a probe is not a deliverable.
public/__*
s6*.mjs
